#include <stdio.h>
main()
{
	int su,max = 0,min = 0;
	printf("®”(~z‚ÅI—¹)");
	while (scanf("%d", &su) != EOF) {
		if (max < su) {
			max = su;
		}
		if (min > su) {
			min = su;
		}
		printf("\n®”(~z‚ÅI—¹)");
	}
	printf("max = %d\n", max);
	printf("min = %d\n", min);
}

